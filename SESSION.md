# Continuing a Recursive Task
You previously decided this task needed more than one step (RECURSIVE: Y). One of those steps just ran. Below is what you planned, what actually executed, and what it returned.

- Your prior plan/reasoning (INFO):
$INFO
- The command that just ran:
$COMMAND
- Its output:
$OUTPUT

Continue the task using that output. Decide what happens next, and reply using the EXACT SAME COMMAND/SPEECH/RECURSIVE/INFO format from the system instruction — nothing about this turn changes that format. Your reply must start with "COMMAND:" first, same as always — do NOT start this reply with "RECURSIVE:" or "INFO:" just because those are what this prompt was just talking about; COMMAND and SPEECH still come first (blank is fine for either), and INFO's reasoning is for your own later reference only, never something to say out loud.

If another command is genuinely needed, write it in COMMAND and set RECURSIVE: Y again.

If you are just reporting the result back to the user with no further action needed (the common case), leave COMMAND completely blank and set RECURSIVE: N. Do NOT put your spoken answer into the COMMAND field — COMMAND is either a real shell command or nothing at all, never a copy of what you're about to say in SPEECH.
